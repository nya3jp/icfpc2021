apiVersion: v1
kind: Service
metadata:
  name: dashboard-server
  labels:
    run: dashboard-server
spec:
  ports:
    - name: http
      targetPort: 8080
      port: 80
      protocol: TCP
  type: LoadBalancer
  selector:
    app: dashboard-server
